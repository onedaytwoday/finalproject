<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="paymeny">
	<select id="review" resultType="MemberDto">
		SELECT REVIEW_NO, REVIEW_TITLE, REVIEW_RATE, REVIEW_DATE, REVIEW_CONTENT, PRODUCT_NO, CLASS_NO, MEMBER_ID
		FROM REVIEW
		ORDER BY REVIEW_NO
	</select>
	
	<select id="selectOne" parameterType="String" resultType="MemberDto">
		SELECT REVIEW_NO, REVIEW_TITLE, REVIEW_RATE, REVIEW_DATE, REVIEW_CONTENT, PRODUCT_NO, CLASS_NO, MEMBER_ID
		FROM REVIEW
		WHERE REVIEW_NO = #{review_no}
	</select>
	
	<insert id="insert" parameterType="MemberDto">
		INSERT INTO REVIEW
		VALUES(#{review_no},#{review_title},#{review_rate},#{review_date},#{review_content},#{product_no},#{class_no},
		#{member_id})
	</insert>
	
	<delete id="delete" parameterType="String">
		DELETE FROM REVIEW 
		WHERE REVIEW_NO = #{review_no}
	</delete>
</mapper>
